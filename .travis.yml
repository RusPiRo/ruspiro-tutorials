# build only master branch on commit's
# all other branches build on PullRequest creation
branches:
  only:
    - master

language: rust

rust:
# build nightly only for the time beeing
  - nightly

matrix:
  fast_finish: false
  include:
    - name: "01_BLINKLED"
      install:
        - sudo apt-get install gcc-aarch64-linux-gnu gcc-aarch64-linux-gnu
        - cargo install cargo-xbuild
        - rustup target add aarch64-unknown-linux-gnu
        - rustup component add rust-src
        - sudo chmod ugo+x ./01_BLINKLED/build.sh
        - cd ./01_BLINKLED
      script: ./build.sh travis

    - name: "02_CONSOLE"
      install:
        - sudo apt-get install gcc-aarch64-linux-gnu gcc-aarch64-linux-gnu
        - cargo install cargo-xbuild
        - rustup target add aarch64-unknown-linux-gnu
        - rustup component add rust-src
        - sudo chmod ugo+x ./02_CONSOLE/build.sh
        - cd ./02_CONSOLE
      script: build.sh travis
